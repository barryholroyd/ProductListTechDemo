<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    >
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:gravity="center"
        android:textSize="30sp"
        android:text="Walmart Products"
        />
    <android.support.v7.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical"
        android:id="@+id/list">
    </android.support.v7.widget.RecyclerView>
    <!-- DEL: delete the following button, button_productinfo, when no longer needed. -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_gravity="center_horizontal"
        android:text="See Product Info (mock)"
        android:id="@+id/button_productinfo"
        android:onClick="buttonTmp"
        />
    <!-- DEL: delete the following button, button_productlist, when no longer needed. -->
    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_gravity="center_horizontal"
        android:text="Scroll Product List"
        android:id="@+id/button_productlist"
        android:onClick="buttonTmp"
        />
</LinearLayout>
